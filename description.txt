Adds a visual speedrun timer in-game.

By default, has support for Any% and AllBosses% run types, but mod authors are able to add their own categories and splits via mod calls.

This timer does not affect gameplay in any way shape or form, and serves only as an automatic (and perfectly accurate) info display.